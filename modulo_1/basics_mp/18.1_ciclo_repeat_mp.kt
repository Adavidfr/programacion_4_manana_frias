fun main() {
    println("Simulando auditoría de accesos diarios (5 intentos):")
    repeat(5) { i ->
        println("Auditoría de Acceso #${i + 1}: Tarjeta de empleado leída correctamente.")
    }
}